// ARRAYS
const vegetables = [];
console.log("I think that the value of the array vegetables is 0 because it is empty, and if we check it's length, it would return zero.");
console.log(vegetables);
const favAnimals = ['whales', 'elephants', 'lions'];
console.log(favAnimals);
const newFavAnimals = favAnimals.unshift('baby pig');
console.log(favAnimals);